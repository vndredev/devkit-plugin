# Development Guide

> Auto-generated from config.jsonc. **Claude MUST read this before making changes.**

## Quick Reference

| Area | Count | Location |
|------|-------|----------|
| Config Sections | 0 | `.claude/.devkit/config.jsonc` |
| Hooks | 5 | `hooks.*` in config |
| Layers | 0 | `arch.layers` in config |
| Managed Files | 14 | `managed.*` in config |

---

## Checklists

### Adding a New Config Section

When adding a new configuration section:

- [ ] **Schema**: Add to `templates/config/config.schema.json.template`
- [ ] **Defaults**: Add to `RECOMMENDED_DEFAULTS` in config handler
- [ ] **Type-specific**: Add to `PROJECT_TYPE_DEFAULTS` if only for certain project types
- [ ] **Health-Check**: Add check function if section needs validation
- [ ] **Docs**: Update relevant skill reference docs

### Adding a New Hook

When adding a new hook:

- [ ] **Config**: Add `hooks.<name>` section to schema
- [ ] **Handler**: Create handler in events layer
- [ ] **Prompts**: Add customizable prompts in config
- [ ] **Tests**: Add tests for handler
- [ ] **Docs**: Document in skill reference

### Modifying Managed Files

When changing managed files:

- [ ] **Template**: Update in `templates/` directory
- [ ] **Schema**: Update `managed.*` in schema if new category
- [ ] **Sync**: Verify with `/dk plugin check`

---

## Active Configuration

### Hooks

| Hook | Status | Config |
|------|--------|--------|
| `session` | enabled | `hooks.session` |
| `validate` | enabled | `hooks.validate` |
| `format` | enabled | `hooks.format` |
| `plan` | enabled | `hooks.plan` |
| `arch_guard` | enabled | `hooks.arch_guard` |

### Architecture Layers

| Layer | Tier | Description |
|-------|------|-------------|
| - | - | - |

### Managed Files

  - `.markdownlint.json` (linters)
  - `.markdownlintignore` (linters)
  - `.github/workflows/claude.yml` (github)
  - `.github/workflows/claude-code-review.yml` (github)
  - `.github/ISSUE_TEMPLATE/bug_report.yml` (github)
  - `.github/ISSUE_TEMPLATE/feature_request.yml` (github)
  - `.github/ISSUE_TEMPLATE/config.yml` (github)
  - `.github/PULL_REQUEST_TEMPLATE.md` (github)
  - `.github/workflows/release.yml` (github)
  - `README.md` (docs)
  - `CLAUDE.md` (docs)
  - `docs/PLUGIN.md` (docs)
  - `docs/DEVELOPMENT.md` (docs)
  - `.gitignore` (ignore)

---

## Key Files

| Purpose | File |
|---------|------|
| Config | `.claude/.devkit/config.jsonc` |
| Schema | `.claude/.devkit/config.schema.json` |
| Main Docs | `CLAUDE.md` |
| Architecture | `docs/ARCHITECTURE.md` |

---

*Generated by devkit-plugin. Run `/dk plugin update` to refresh.*
