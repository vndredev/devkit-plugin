{
  "name": "devkit-plugin",
  "version": "0.17.3",
  "description": "Claude Code Plugin for automatic code standards",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "markdownlint-cli": "^0.47.0",
    "prettier": "^3.4.0"
  }
}
